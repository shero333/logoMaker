<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TheLogoMaker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TheLogoMaker" parent="Base.Theme.TheLogoMaker" />

    //Splash
    <style name="Theme.AppSplash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/theme_color</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/app_icon</item>
        <item name="windowSplashScreenAnimationDuration">7000</item>
        <item name="postSplashScreenTheme">@style/Theme.TheLogoMaker</item>

    </style>

    //bottom navigation text font
    <style name="CustomBottomNavigationText">
        <item name="android:fontFamily">@font/font_text</item>
        <item name="android:textSize">11sp</item>
    </style>

    //seekbar style
    <style name="CustomSeekBar">
        <item name="android:progressDrawable">@drawable/custom_seekbar</item>
    </style>
    <style name="ColorPickerDialogStyle" parent="Theme.AppCompat.DayNight.Dialog.Alert">
        <!-- Customize the text color for positive (Select) and negative (Cancel) buttons -->
        <item name="android:background">@color/black</item>
        <item name="android:textColor">@color/white</item>
        <item name="textFillColor">@color/white</item>
    </style>

    <!--rating bar view-->
    <style name="logoRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/rating_bar_view</item>
        <item name="android:minHeight">23dip</item>
        <item name="android:maxHeight">25dip</item>
    </style>

    <style name="AdTheme" parent="Base.Theme.TheLogoMaker">
        <item name="android:background">@color/black</item>
        <item name="android:windowBackground">@color/black</item>
        <item name="android:windowExitAnimation">@null</item>
        <item name="android:windowEnterAnimation">@null</item>
        <item name="android:statusBarColor">@color/theme_color</item>
    </style>
</resources>